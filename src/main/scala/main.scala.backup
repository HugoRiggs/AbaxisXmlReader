object main extends App {
  require(args.length == 1)

  // load xml
  val loadnode = xml.XML.loadFile(args(0))

    // create banner
  println(Parser.Xml(loadnode).patientName + ", analyte Results:\n"
    + "|Name|\t|Value|\t|Low|\t|High|\t|Mark|")


  // print results
  Parser.Xml(loadnode).analyteResults.foreach(result => println(result))
 
}
